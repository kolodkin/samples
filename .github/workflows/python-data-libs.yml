name: python-data-libs

on:
  workflow_dispatch:
  push:
    paths:
      - 'python-data-libs/**'

jobs:
  benchmark:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: python-data-libs
    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v4

      - name: Install dependencies
        run: uv sync

      - name: Run data library benchmark
        run: |
          uv run python -m python_data_libs | tee /tmp/data_bench.txt
          echo '## Data Library Benchmark' >> "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"
          cat /tmp/data_bench.txt >> "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"
