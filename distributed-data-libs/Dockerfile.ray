# Official Ray image: python:3.11-slim is missing native libs the
# dashboard agent (Jobs API server) needs to spawn. See SPEC.md.
FROM rayproject/ray:2.48.0-py311

USER root
WORKDIR /app

COPY distributed_data_libs ./distributed_data_libs

ENV PYTHONPATH=/app
ENV FRAMEWORK=ray
